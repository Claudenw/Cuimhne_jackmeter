name: build_package
on: 
    push: 
        branches: [ main ]
jobs:
    build:
        name: build
        runs-on: self-hosted
        steps:
        - name: Checkout Code
          uses: actions/checkout@v3
          
        - name: Configure Code
          run: ./configure

      